#include "AddTwoNumbers.h"
#include "MedianofSortedArrays.h"

/*int main() {
    Solution s;
    ListNode *l1 = makeNode(9999);
    printList(l1);

    ListNode *l2 = makeNode(9999);
    ListNode* l3= s.addTwoNumbers(l1,l2);
    printList(l2);
    printList(l3);

    return 0;
}

int main ()
{
    MedianofSortedArrays m;
    std::cout << "median : " << m.findMedianSortedArrays(std::vector<int> {1,2,3,4,5},std::vector<int>{6,7,8,9,10});
}

int main()
{
    //print true if bits are alternating.
    int a = 4; int p = a&1;
    cout<<"a"<< a <<"p" << p;
    a=a>>1;
    cout<<"a" << a << " ";
    while (a != 0)
    {
        if (p == (a&1))
        {
            std::cout << false;
            return false;
        }
        p = a&1;
        a=a>>1;
    }
    std::cout << true;
}
*/

